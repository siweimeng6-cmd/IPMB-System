1.2025.6.16
*PB5,PA15开漏拉高输出，可实现menu切换
*由软件来控制上电
*8801配置
*增加盒盖触摸相关的检测void CLOSE_Task(void* parameter)

2.2025.6.23
*删除BL_EN的相关部分，不再由mcu控制
*删除6710红灯相关的部分，红灯代表无“无信号”，并不能表示切换通道的反馈，因此删除
	当切换信号发出后，默认切换成功，不再读取6710的通道（无法读取）。
*使用task读取PC0的高低电平，连续两次低电平触发切换
	每次触发切换后，3s不再使能PC0，由hdmi1_switch_en控制
	
3.2025.6.24
删除切换6710的脉冲信号

4.2025.7.4
版本号V1.0
将8801的teminal resistor 和swing参数调整到最大

5.2025.7.21
版本号V1.1
客户反应切换过慢，验证后，需要在切换期间关闭USB34_EN
	因此在切换电平发出后，拉高USB34_EN,并置位flag
	检测flga==1且USB34_EN为高3.5s后，再次拉低USB34_EN，并清除flag
	
6.2025.7.22
客户反应，高温时，HDMI切换仍延迟高，尝试再次延长HDMI切换时USB34_EN断电时间，延长至6s

7.2025.7.25
客户反应，上电时喇叭有噗声音，因此上电延时2000ms使能P5V_EN，
示波器实测5V上电晚于MCU_3V3仅1s
另外bsp_init.c中，增加extern void vPortSetupTimerInterrupt( void );

8.2025.7.29
由于P5V_EN上电延迟，导致了KVM03的万兆异常
因此还原P5V_EN的延迟，增加PA0的使能控制功放
PA0开漏模式默认为高，上电完成后（P3V3_PG）后500ms，拉低功放使能PA0

9.2025.7.31
将喇叭功放拉低延时到30s以后，即3.3v_PG后30s
后又更改，将PA0跟随PD2变化，PD2信号由CPLD给出

10.2025.8.1
gpio初始化时，增加GPIOD的时钟使能

10.2025.8.5
由MCU来控制背光，在合盖时关闭背光，开盖打开背光
删除PB5,PA15的初始化，切换功能不再使用


11.2025.8.20
潘工修改需求：PC6做输入，低电平关背光PC7高电平打开；PC8做输入，低电平关背光PC7高电平打开
需要考虑开盖合盖的状态，否则出现闪屏

11.2025.8.21
将PC6,PC8改为由低脉冲触发，第一次低脉冲开背光，再开一次关背光









26年
********************************************************************************************************
12.2026.01.12
PA0：
1.bsp_gpio.h
第10-11行：添加了 NS4251_SD_R_GPIO_PORT 和 NS4251_SD_R_GPIO_PIN 定义，对应PA0引脚
第13-14行：添加了 MCU_GPIO0_GPIO_PORT 和 MCU_GPIO0_GPIO_PIN 定义，对应PD2引脚

2. bsp_gpio.c
第82-92行：添加了PD2和PA0的GPIO初始化配置
	   PD2 (MCU_GPIO0) 配置为输入模式，默认上拉
	   PA0 (NS4251_SD_R) 配置为推挽输出模式，默认上拉

3. task_gpio.c
第202-212行：添加了PD2状态检测和PA0控制逻辑
	        当PD2为低时，PA0输出低
	        当PD2为高时，PA0输出高

4. bsp_init.c
更新了编译日期为2026年1月12日

修改说明
1. PA0 (NS4251_SD_R) ：配置为推挽输出模式，默认上拉，当PD2为低时输出低，用于控制扬声器功放使能。
2. PD2 (MCU_GPIO0) ：配置为输入模式，默认上拉，作为系统输入信号，计算进系统后输出低给单片机。

PA8：（功能已删）
1. bsp_gpio.h
第15-18行：添加了 MCU_GPIO2_GPIO_PORT 和 MCU_GPIO2_GPIO_PIN 定义，对应PA8引脚
2. bsp_gpio.c
第95-99行：添加了PA8 (MCU_GPIO2) 的GPIO初始化配置，设置为输入模式，默认上拉
3. task_gpio.c
 第47行：添加了 mcu_gpio2_cnt 变量，用于计数PA8低电平持续时间
第254-273行：添加了PA8 (MCU_GPIO2) 状态检测和触摸功能控制逻辑
 当检测到低电平时，计数递增，达到100（5秒）时关闭触摸功能
 当检测到高电平时，重置计数并打开触摸功能

修改说明
1. PA8 (MCU_GPIO2) ：配置为输入模式，默认上拉，用于检测盒盖状态
2. 触摸功能控制 ：当PA8接收到低电平持续5秒时，PB1 (USB34_EN) 输出高关闭触摸功能；当PA8为高电平时，PB1输出低打开触摸功能
3. 计数逻辑 ：使用 mcu_gpio2_cnt 变量，每次循环增加1，当达到100时（每个循环100ms，共5秒）执行关闭触摸功能的操作







260311
已完成LED闪烁灯   警告灯为测试写的闪烁逻辑  电平控制正常  串口打印健康信息正常  0X50地址的FLASH没写  IPMB预留功能没写

260312
新增CPU'通信串口 ？ 指令待测试


260312下午  测试PC4-PWR_CTL
poweroff 下电指令 测试成功
































