 [Unit]
Description=Temperature Monitor Service to /dev/ttyS3
After=network.target

[Service]
Type=simple
# 修正：必须写绝对路径 + 给脚本执行权限
ExecStart=/bin/bash /usr/local/bin/temp_monitor.sh
# 重要：防止无限重启（脚本正常退出也不重启）
Restart=on-failure
RestartSec=5
User=root
 
[Install]
WantedBy=multi-user.target